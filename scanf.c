#include <stdio.h>
    int main (){
    int length ;
    int width;
    printf ("Enter a width: ");
    scanf (" %d", &width);
    printf ("Enter a length: ");
    scanf (" %d", &length);
    printf("The area of the rectangle is: %d", length*width);

    return 0;

    }
